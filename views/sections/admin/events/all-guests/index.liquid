<div class="flex flex-col w-full md:w-[545px] bg-paper border-x border-black-500 min-h-[100vh] py-[144px] font-sans px-[24px] md:px-[55px]">
    <h1 class="mt-20 text-7xl text-primary font-serif mb-4">All Guests</h1>
    <div class="my-4">
        <h2 class="text-2xl font-semibold">{{ event.name }}</h2>
        <a href="/admin/events/{{ event.id }}" class="text-blue-600 hover:underline">&larr; Back to Event</a>
    </div>
</div>
