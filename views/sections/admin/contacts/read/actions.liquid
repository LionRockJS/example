<div class="order-last z-20">
    <div class="flex panel-space">
          <a href="/admin/contacts/edit/{{ item.id }}"
             class="text-center block cursor-pointer transition duration-500 w-full rounded-lg py-2 uppercase text-base leading-8 font-sans text-white {% if published %}bg-gray-700 hover:bg-gray-900{% else %}bg-primary hover:bg-highlight{% endif %} focus-visible:outline-offset-2 focus-visible:outline-primary"
          >Edit</a>
    </div>
</div>