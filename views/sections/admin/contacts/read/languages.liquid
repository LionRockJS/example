{% assign controller_slug = section.settings.controller_slug | default: "pages" %}

<div class="sticky top-4 mb-6 z-10">
<div class="mx-[4px] inline-flex backdrop-blur rounded-full">
  <span class="text-primary min-w-[16px] md:min-w-[47px] me-[4px] flex justify-center items-center"><i class="fa-sharp fa-light fa-globe"></i></span>

  <div class="value-language-selector inline-block pe-4">
    {% if language == "mis" or language == blank %}<span class="text-primary"><i class="fa-solid fa-book-open"></i></span>{% else %}<a href="/admin/{{ controller_slug }}/{{ item.id }}?language=mis"><i class="fa-light fa-book-open"></i></a>{% endif %}
    {% if language == "en"      %}<span class="font-bold border-b-2 border-primary text-primary">EN</span>{% else %}<a href="/admin/{{ controller_slug }}/{{ item.id }}?language=en">EN</a>{% endif %}
    {% if language == "zh-hant" %}<span class="font-bold border-b-2 border-primary text-primary">繁</span>{% else %}<a href="/admin/{{ controller_slug }}/{{ item.id }}?language=zh-hant">繁</a>{% endif %}
    {% if language == "zh-hans" %}<span class="font-bold border-b-2 border-primary text-primary">简</span>{% else %}<a href="/admin/{{ controller_slug }}/{{ item.id }}?language=zh-hans">简</a>{% endif %}
  </div>
</div>
</div>